---
name: memory-learning
description: >
  Merged into memory-protocol. Do not load this skill — use memory-protocol instead,
  which now includes both the tool API and the direct SQL/schema access patterns.
metadata:
  keywords: "memory, sql, postgres, vector, embedding"
  category: "memory"
  load: "never"
---

> **Deprecated.** Content merged into `skills/memory-protocol/SKILL.md` under the
> "Advanced: Direct Database Access" section. Load `memory-protocol` instead.
