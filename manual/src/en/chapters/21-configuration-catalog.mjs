export const configurationCatalog = {
  id: 'chapter.configuration-catalog', number: 21, title: 'Configuration catalog',
  purpose: 'Use the complete generated registry to locate a key, then apply the safety and precedence guidance owned by Chapter 11.',
  audiences: ['Owner', 'Operator', 'Integrator', 'Contributor'],
  applicability: { release: 'All 112 registry keys parsed from pinned lib/config.js.', interfaces: 'Generated Field Console rows plus canonical configuration procedure.', evidence: 'Key, section, type, tier and source are generated. Explanations and warnings are authored overlays.' },
  sections: [
    { id: 'concept.catalog-navigation', title: 'The catalog answers what exists, not what you should change', paragraphs: ['Search by exact key when known; otherwise narrow by section and type. Tier 0 means bootstrap or secret and environment-only. Tier 1 may be Settings-managed. Return to Chapter 11 before changing any value.', 'Defaults, examples, and visibility are not recommendations for every deployment. Feature gates, credentials, storage identity, model sizing, retention, and network controls require their owning chapter.'], rules: ['Never copy the catalog into another maintained table.', 'Do not expose secret values.', 'Resolve provenance after restart.', 'Treat unknown keys as unsupported until added to the registry.'] },
    { id: 'concept.catalog-change-risk', title: 'Group keys by effect before editing', paragraphs: ['Low-risk display or optional gates can still require restart. Provider, database, path, shell, encryption, embedding, network, and retention keys cross durable or security boundaries. A key’s type does not express all operational risk.', 'When a generated fact conflicts with prose, stop: either the prose is stale or the pinned source contains a contradiction. Do not override the row editorially.'] }
  ],
  procedures: [{ id: 'procedure.use-config-catalog', title: 'Resolve one key from catalog to runtime behavior', audience: 'Operator or contributor', goal: 'One configuration key is identified, classified, traced to provenance, and routed to its owning safe-change procedure.', prerequisites: ['Exact pinned edition', 'Complete generated configuration rows', 'Access to effective configuration provenance'], platforms: 'Environment syntax varies; registry meaning is common.', warning: 'Do not change a value merely to discover its effect when source and owning chapter can answer first.', start: 'State the intended outcome rather than a guessed key.', steps: [{ action: 'Search the generated rows for exact or related keys and record section, type, tier, and source.' }, { action: 'Read the key’s pinned registry entry for default, options, description, and risk metadata.' }, { action: 'Identify current effective value and provenance without printing a secret.' }, { action: 'Route to Chapter 11 or the domain-owning chapter and evaluate restart, data, and rollback impact.' }, { action: 'If authorized, perform one reversible synthetic change through the canonical procedure.' }, { action: 'Restore and verify provenance; leave the generated catalog untouched.' }], success: 'The key and its runtime owner are identified with no shadow fact or secret disclosure.', result: 'Configuration work begins from the registry rather than folklore.', recovery: ['If no row exists, do not invent the key.', 'If prose conflicts, escalate the contradiction.', 'If provenance is unavailable, restart only after recording all layers.'], reversal: 'Restore through Chapter 11 and remove test-layer residue.', next: ['chapter.configure', 'chapter.privacy-security', 'chapter.commands-checks'], returns: ['role.operator', 'role.contributor'] }],
  generatedProjection: { id: 'projection.chapter-21-facts', title: 'Configuration registry', query: { family: 'config' } }
};
