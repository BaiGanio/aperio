export const capabilityCatalog = {
  id: 'chapter.capability-catalog', number: 23, title: 'Tools, providers, integrations, and capability catalog',
  purpose: 'Locate a reachable capability and then apply its trust, data, credential, mutation, and availability boundary.',
  audiences: ['Owner', 'Integrator', 'Operator', 'Contributor'],
  applicability: { release: 'Root MCP tool modules and six configured provider lanes at the pinned release.', interfaces: 'MCP tools, browser/terminal provider loops, local and external dependencies.', evidence: 'Registration proves reachability. Runtime availability still depends on feature gates, credentials, paths, models, services, and client exposure.' },
  sections: [
    { id: 'concept.capability-status', title: 'Reachable is not available, authorized, or safe', paragraphs: ['A registered tool may reject because a feature is off, a path is outside scope, a credential is absent, a connection is read-only, a model lacks vision, or confirmation is pending. Those are distinct and useful outcomes.', 'Providers share orchestration goals but differ in credentials, tool schema translation, streaming, cancellation, and local/cloud data boundaries. One passing provider or interface does not verify another.'], rules: ['Start with discovery and read-only calls.', 'Apply least authority.', 'Inspect external egress and untrusted input.', 'Treat confirmation and cancellation as lifecycle states.', 'Record unavailable dependencies honestly.'] },
    { id: 'concept.capability-owner', title: 'Route from catalog row to canonical chapter', paragraphs: ['Memory and wiki route to Chapter 3; sessions and agents to Chapter 4 or 9; files and artifacts to Chapter 5; graphs to Chapter 6; client connection to Chapter 7; external integrations to Chapter 8; privacy to Chapter 13.', 'The catalog must remain complete even when a capability lacks a narrative example. Missing prose is not permission to improvise a procedure.'] }
  ],
  procedures: [{ id: 'procedure.assess-capability', title: 'Assess one capability before use', audience: 'Owner, integrator, or operator', goal: 'One tool/provider request is mapped to prerequisites, data flow, mutation gate, success evidence, failure behavior, and cleanup.', prerequisites: ['Exact capability row', 'Owning chapter', 'Known installation/provider/client lane'], platforms: 'Capability availability is environment-specific.', warning: 'Do not provide credentials, broaden paths, enable shell, or approve mutation to turn an unavailable capability into a passing demo.', start: 'State the desired outcome, data classification, target system, and maximum acceptable authority.', steps: [{ action: 'Find the generated tool or provider row and its pinned source.' }, { action: 'Identify feature gate, dependency, credential, allowed path, egress, and provider boundary.' }, { action: 'Identify whether the action reads, writes, queues, proposes, confirms, or runs in background.' }, { action: 'Run the smallest synthetic read-only probe or record the dependency as unavailable.' }, { action: 'If mutation is required and authorized, follow the owning canonical procedure and inspect exact confirmation.' }, { action: 'Verify result, cancellation/failure behavior, retained data, and cleanup.' }], success: 'The capability is used or declined with a complete boundary record.', result: 'Catalog discovery does not outrun authority or evidence.', recovery: ['On unavailable dependency, stop.', 'On unexpected tool exposure, inspect client/provider allowlists.', 'On ambiguous mutation state, verify at the target before retry.'], reversal: 'Reject pending actions, revoke temporary credentials, remove synthetic state, and restore gates.', next: ['chapter.integrations-external-data', 'chapter.agents-automation', 'chapter.privacy-security'], returns: ['role.owner', 'role.integrator'] }],
  generatedProjection: { id: 'projection.chapter-23-facts', title: 'Capability catalog', query: { family: 'mcp' } }
};
