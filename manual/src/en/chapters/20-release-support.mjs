export const releaseSupport = {
  id: 'chapter.release-support', number: 20, title: 'Release and support matrix',
  purpose: 'Read release identity, evidence status, and environment-specific results without promoting a successful example into universal support.',
  audiences: ['Everyday user (owner)', 'Operator', 'Integrator', 'Contributor'],
  applicability: { release: 'This edition describes v0.68.0 at its pinned commit.', interfaces: 'Source install, browser, terminal, stdio MCP, SQLite/Postgres, local/cloud providers and external clients.', evidence: 'Verified means directly established at the pin; present-unverified means shipped or named but not release-matrix proven; unsupported means absent or contradicted.' },
  sections: [
    { id: 'concept.support-status', title: 'Presence, verification, and support are different claims', paragraphs: ['A command in package.json proves a shipped entry point. A passing synthetic flow proves that exact checkout and environment. Neither alone establishes every operating system, runtime, client, provider, database, deployment, or upgrade path.', 'Read each row with its source, status, and limitation. Environment results may strengthen a lane only when their runtime, platform, dependency versions, and complete affected flow are recorded. Silence is unknown, not support.'], rules: ['Never replace an evidence label with marketing language.', 'Keep product SHA and environment result together.', 'Reconcile contradictory pinned files explicitly.', 'Do not infer client support from MCP protocol compatibility.'] },
    { id: 'concept.release-identity', title: 'The release pin owns factual identity', paragraphs: ['Tag, package version, commit, and release ref must agree before facts are generated. Mutable README, current docs, issue prose, or a newer branch can explain intent but cannot silently overwrite the pinned product.', 'The matrix is read-only generated data. This chapter owns interpretation and routes; edits belong in source authority or explicit editorial overlays, never in rendered rows.'] }
  ],
  procedures: [{ id: 'procedure.read-support-matrix', title: 'Decide whether one requested lane is evidenced', audience: 'Any reader planning use or deployment', goal: 'One platform/client/provider/backend request is classified as verified, present-unverified, unsupported, or externally blocked from pinned evidence.', prerequisites: ['The exact requested lane', 'The generated release/support rows', 'Any environment-specific proof with full identity'], platforms: 'The procedure compares platforms; it does not assume one.', warning: 'Do not spend money, migrate data, or expose a service based on a present-unverified row.', start: 'Write the requested combination of release, platform, runtime, interface/client, backend, provider, and deployment.', steps: [{ action: 'Match every dimension to a generated row or named environment result.' }, { action: 'Open each canonical source and retain its evidence status and limitation.' }, { action: 'Reconcile contradictions; the narrower or safer claim wins until direct proof exists.' }, { action: 'Classify the whole lane by its weakest required dimension.' }, { action: 'If proof is feasible, run the canonical isolated affected flow and attach complete identity.' }, { action: 'Record the decision and link to the owning installation or connection procedure.' }], success: 'The decision is traceable and makes no broader claim than its weakest dependency.', result: 'The reader knows whether to proceed, test, or stop.', recovery: ['If rows conflict, stop and preserve both sources.', 'If identity is missing, treat the result as anecdotal.', 'If a required service is external, name it as a dependency rather than product support.'], reversal: 'Discard only the synthetic environment proof and retain the original matrix unchanged.', next: ['chapter.install-deploy', 'chapter.configuration-catalog', 'chapter.evidence-escalate'], returns: ['role.owner', 'role.operator', 'role.integrator'] }],
  generatedProjection: { id: 'projection.chapter-20-facts', title: 'Release and support rows', query: { ids: ['release.0-68-0', 'support.node-source-install', 'support.client-specific-fields', 'support.provider-interface-divergence', 'data.postgres-compose-boundary'] } }
};
